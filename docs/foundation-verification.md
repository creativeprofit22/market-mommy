# Foundation implementation evidence

Status: implementation in progress, not a release or completed Roadmap phase. Synthetic fixtures only. No live providers, customer records, host registration, commits or publishing authorized.

## Step 1 — runtime and dependency review (2026-09-19)

The existing modified `AGENTS.md`, untracked `CONTEXT.md` and canonical documents match the approved preservation boundary. `docs/brainstorming/` is untouched. Starting HEAD: `b4a4fae`.

**Runtime observed:** PATH initially used Node 22.20.0. Existing fnm default was 24.19.0. The user explicitly authorized side-by-side installation of 24.21.0 without changing the default. `fnm install 24.21.0` succeeded; `fnm list` retained 24.19.0 as default. A 24.21.0 probe opened `DatabaseSync` with `timeout: 100`, queried SQLite 3.53.4 and confirmed the `backup` function export. This is API availability, not a persistence/recovery test. SQLite maturity remains the approved risk.

**Chosen direct pins:** `@kenkaiiii/gg-agent@5.60.3`, `@kenkaiiii/gg-ai@5.60.3`, `@modelcontextprotocol/sdk@1.30.0`, `zod@4.5.4`, development `typescript@6.0.3` and `@types/node@24.12.0`. No substitution. npm 10.9.3 resolved an inspection-only lock in ignored `.gg/dependency-review/`, using `--package-lock-only --ignore-scripts`. It reported 109 audited packages and zero known vulnerabilities. This is a dated advisory result, not a security certification.

**Archive review:** downloaded all 108 locked dependency archives from the npm registry, checked each against its locked integrity, bounded compressed/expanded sizes, rejected traversal/link members, and inspected package manifests, license-file inventory and lifecycle scripts without executing package code. Declared licenses: MIT, ISC, Apache-2.0, BSD-2-Clause, BSD-3-Clause and Unlicense. Eight installed-path manifests declare `prepare` scripts (content-type in three locations, eventsource, express-rate-limit, ip-address, path-to-regexp and standardwebhooks). No preinstall/install/postinstall hooks found. Scripts remain disabled, including ip-address's git-config-changing prepare hook.

Two archives omit license files: `standardwebhooks@1.1.1` and `@bokuweb/zstd-wasm@0.0.27`. Both declare MIT. Standardwebhooks's actual library MIT notice was inspected at registry-reported gitHead `b4d2c14fc5b4ccff3ff271e3b087dff812254c59`, `libraries/LICENSE`; the repository root instead licenses the specification under Apache-2.0. zstd-wasm's registry-reported gitHead `87277ab93c2861d6c265a2ff43c7e7fb14d9a8c5` has no license-named file in its recursive tree. Local fixture development relies on its package license declaration; complete wrapper/WASM upstream notices and provenance must be resolved before any redistribution. No redistribution is authorized or performed. Full implementation audit of every transitive dependency is not claimed.

**Packaged API/code inspection:** gg-agent's built ESM and declarations expose Agent, agentLoop, typed tools, cancellation, turn/token/tool-result limits, turn-extension and pause-continuation controls. These match the necessary local-fork seams, not the entire fork. Differences exist (including timeout handling and credential/service-tier options); artifact equivalence is not claimed. Built loop code has internal retry branches and up to two output-token continuations independent of `maxContinuations`. Its local-backend first-event watchdog is disabled. Therefore the owned adapter (subsequently implemented in steps 5–8) must enforce overall deadlines, aggregate output limits and finite child termination independently; no monetary guarantee follows from framework settings. No live calls are permitted.

Review artifacts and extracted source are ignored local agent state, not committed third-party source. The application lockfile preserves resolved versions and integrity. Archive review used Python 3.13's standard tar reader; no inspection tooling dependency was installed.

## Steps 2–4 — implemented and verified (2026-09-19)

- **Package foundation:** strict TypeScript/ESM, exact runtime/direct pins, lockfile, disabled lifecycle scripts and import-boundary check. All dependency entries in the root lockfile matched the reviewed archive lock before `npm ci --ignore-scripts`. Installed public Agent/MCP exports load; this is not execution/protocol verification. Runtime checks reject using another Node version for build/typecheck/test.
- **Domain/application contracts:** runtime schemas for profiles, observations, evidence, interpretations, recommendations/abstentions, offers, experiments/outcomes, Journeys and jobs. Exact integer-text money survives values above JavaScript's safe integer range. Expected versions, immutable experiment baselines, terminal job states, bounded caller limits, strict input envelopes, opaque launch capabilities and safe error messages have tests. Input parsing enforces byte and nesting bounds. At this intermediate checkpoint no dispatcher/use cases existed; steps 5–8 below subsequently added them.
- **Scenario inventory:** `tests/fixtures/scenarios.json` predeclares 30 non-sensitive scenarios in the accepted 10/10/10 groups. Expected product decisions/actions are labels, not passing recommendations. Quality remains `unevaluated`; the inventory test checks allocation and labels only.
- **Storage:** asynchronous typed storage port over an owned SQLite worker, sanitized environment, bounded request size/pending count and finite request timeout. Local absolute paths only; no SQL transport operation. Worker uses WAL, foreign keys, full synchronous writes, disabled extensions and a busy timeout. Checked-in, checksum-tracked forward migrations establish identities, append-only record versions, dependencies, receipt storage and removal-tombstone storage. Receipt/tombstone tables do not mean their application workflows are delivered.
- **Real-file checks:** close/reopen preserves versions; a failed multi-record/dependency transaction rolls back; two independent workers have one expected-version winner; write-lock contention fails without losing prior records; populated schema-1 upgrade backs up with SQLite's backup API and applies schema 2; repeat migration is a no-op; failed migration rolls back; changed checksums/future versions are refused. A child process was killed inside an actual migration transaction, then the store reopened with the old schema and records. Invalid directory destinations and unrelated databases fail rather than being recreated. SQL constraints independently reject broken references and history rewrites.

**Historical steps 2–4 checks:** Node 24.21.0 on Windows, `npm run typecheck`, `npm test` (includes `npm run build`), and `npm run check:boundaries` passed. **35 tests passed, zero failed/skipped; 18 source files checked.** The earlier application-boundary false positive was corrected to permit the shared Zod schema library; transport/SQL/framework imports remain forbidden inward. The initial migration-backup count assertion included WAL/SHM sidecars; it now counts backup database files specifically, retaining the one-backup assertion.

### Reproducing the current checks

Select the version in `.node-version` in the current shell only, then run:

```sh
npm ci --ignore-scripts
npm run typecheck
npm test
npm run check:boundaries
```

On this Windows host, `fnm exec --using=24.21.0 -- node E:/nodejs/node_modules/npm/bin/npm-cli.js <npm arguments>` was used because direct `fnm exec npm` could not launch the npm command shim. This machine-specific path is not portable setup guidance. Existing fnm default and PATH runtime were not changed. The CLI and MCP launch commands now exist; see `examples/README.md` for absolute-path launch and fixed capability configuration. Enqueueing does not automatically execute fixture jobs; the trusted `runNextFixture` API is separate.

## Steps 5–8 — implementation continuation (2026-09-19)

**Shared use cases:** one validated dispatcher runs authorization, expected-version checks, domain writes and semantic request receipts inside the owning worker's SQLite transaction. Exact receipt replay survives restart; a changed payload or Journey conflicts. Profile/evidence corrections append versions and flag dependent interpretations/recommendations in all affected Journeys. Fixture advice is explicitly supplied synthetic data, not generated market advice. Experiment baselines stay immutable and outcome corrections append nonbranching supersessions. The three contract traces now have real-file assertions; workflow-specific assertions are separate. Collections scanned for cross-record checks are capped at 2,000 identities per kind; exceeding the cap fails closed.

**Durable workflow:** migration 003 adds immutable attempts, an append-only synthetic cost ledger, administrative receipts and a singleton claim/fence. Enqueue reserves a synthetic amount against the original run and Journey allowances owned by [evaluation](evaluation.md#accepted-targets). Queue wait consumes the deadline; queue size is 16. Unknown charges keep reservations; settlement replaces the encumbrance. Expired claims quarantine work; uncertain terminal jobs prevent another claim until explicit reconciliation. Linked retry retains its root identity and caps. Trusted administrative reconciliation is not an MCP tool. Real competing-process, process-kill, stale-fence, concurrency, replay and budget tests pass. Jobs inserted as legacy test fixtures without accounting membership cannot silently acquire budget headroom.

**Actual framework execution:** the installed `agentLoop` runs in an owned child with fixed executable/script, sanitized environment and a parent-owned scripted loopback provider. Only immutable allowlisted evidence is readable. Turn, call, tool-result, aggregate output and independent deadline controls are applied; abort has a 250-ms grace then exact-child kill, with cleanup awaited. Scripted 500 responses observed four requests/three failed protocol responses and a framework retry. Migration 004 stores validated fixture output atomically with settlement. No live provider exists. Local zero-cost settlement is known only for the owned fixture protocol, never a paid-provider guarantee.

**Credential/runtime checks:** the worker reports only its environment key count, never names/values. Windows injects 11 baseline keys even with `env: {}`; tests independently assert those exact baseline names, compare the actual framework worker count, and verify inherited nonsecret sentinels are absent from outputs/errors. This is not an OS sandbox certification. At this checkpoint cancellation tests observed durable running state without proving HTTP readiness. Step 10 subsequently replaced fixed sleeps with observable worker/provider readiness and exercised actual request/cancel cycles (details below). Force-stop is tested against an intentionally noncooperative child.

**Interfaces:** real CLI children and actual MCP SDK stdio clients invoke the shared dispatcher from an external working directory. Thirteen strict named tools expose domain commands only; the startup store and capabilities cannot be changed by arguments. Read-only is default, including denial of write-receipt replay. Input is capped at 256 KiB before parsing; the CLI input deadline is five seconds. MCP caps outstanding requests/output and sanitizes protocol errors. Exact parity uses alternating shared-store writes and original persisted receipts replayed from two SQLite backup copies, without normalizing timestamps or IDs. Host registration permission was subsequently requested but dismissed; no settings changed and actual host smoke testing remains unverified. `examples/gg-coder-mcp.json` is inert and disabled.

**Historical steps 5–8 evidence:** complete build/test run reported 72 passed, zero failures/skips. Parent separately reran the three real interface integration tests successfully, plus the 32-file import-boundary check and `git diff --check`. Parent's preceding full typecheck/build/test run passed all 69 then-existing tests, including the real framework checks. All execution so far is Windows/Node 24.21.0; no Linux claim. No commits, live calls, publishing or global host configuration changes.

## Steps 9–11 — storage, verification and documentation

Steps 9–10 are implemented locally. Step 11 reconciles setup, architecture, contracts, operations, decisions, references and evaluation without changing original product requirements, manual Journey traces or brainstorming. This is implementation work, not a Done phase claim.

Migration 005 adds administration/removal-ledger support. The async trusted worker administration API implements consistent SQLite backup, validated semantic export/import and checksum-required backup recovery to exclusively new destinations, including mandatory authoritative removal-ledger reapplication. No existing store is overwritten; uncertain jobs/accounting survive without execution. Corrupt artifacts, reference/checksum failures and removed inputs fail closed. Fixture removals redact dependent payloads transitively, sanitize all saved request/worker results and framework outputs, and permanently reserve/block used keys. Version identities, accounting and workload membership remain. This deliberately sacrifices unrelated old receipt replay; it is not secure erasure or a real-data privacy policy. See [operations](operations.md#implemented-fixture-administration) and [administrative API](storage-administration.md).

CI now configures Windows/Linux build/typecheck/test/boundary checks on the pinned runtime. Action refs were parent-verified against GitHub on 2026-09-19: checkout `3d3c42e5aac5ba805825da76410c181273ba90b1`, setup-node `249970729cb0ef3589644e2896645e5dc5ba9c38`. No hosted run, Linux execution, commit or push is claimed.

## Latest parent verification and measurements

These are parent-provided execution results, not commands rerun during this documentation-only update. On Windows/Node 24.21.0, execution `64cb2f1a-4edb-4ddc-989e-367f6352fea4` passed build, the affected 18 tests, the complete 80-test suite (zero failures/skips), and boundaries over 36 source files. Earlier full-suite execution `009cb9c0` had four timing failures. Fixed sleeps were replaced by observable worker/provider readiness; normal-success child exit is awaited within the job deadline before forced teardown. Assertions were not weakened. `runNextFixture`'s `onProgress` callback is trusted-only (`worker-ready` / `provider-request`), never model-supplied arguments. Historical typecheck results above are not a claim of an additional final typecheck execution.

Report `0fec60cf-282e-45eb-a087-561824c0a37a` passed, finishing **2026-09-19T07:03:23.825Z**:

- Retrieval: 30 cold CLI + 30 warm MCP attempts, zero failures, nearest-rank p95 rank 29; cold **506.4191 ms**, warm **7.8894 ms**. Retrieval caused no state mutation. Store: 102 records / 152 versions, main 4,096 bytes / WAL 3,699,792 bytes. Two sources and 50 evidence records, zero directions/experiments: partial maximum workload, not full product-workload validation. [Evaluation](evaluation.md) alone owns numerical targets.
- Resource exercise: five actual request/cancel cycles passed. Parent RSS **91,123,712 → 105,676,800 bytes**, heap **17,581,944 → 18,371,336 bytes**; resource count **3 → 3** (MessagePort 1, PipeWrap 2). Intentional jobs grew the store to 107 records / 182 versions. Child peak memory and OS handle counts were not measured; stable resource counts do not prove leak freedom.
- Recovery: backup **65.8142 ms**, restore **262.3359 ms**, backup, restore and semantic comparison together **716.4681 ms**; exact semantic tables matched and temporary teardown was true. These fixture timings do not establish real-data recovery objectives or encrypted backup behavior.

`docs/foundation-measurements.json` is retained unchanged as a **historical earlier successful 60-sample report**, before the readiness fix (cold p95 799.72 ms / warm 7.20 ms). It is not the latest raw sample artifact. Latest values above have execution-ID provenance only; no invented samples or replacement measurement JSON were committed. Fixture resource and timing results do not prove live latency, monetary enforcement, recommendation quality or user benefit.

## Six-criterion evidence and gap matrix

| Approved foundation criterion | Implemented / local evidence | Remaining gate or limitation |
| --- | --- | --- |
| Runnable modular TypeScript core and shared records | Strict schemas, dispatcher, SQLite ports/migrations, build and 36-file boundaries; full 80-test suite | Fixture-only domain paths; later reasoning/product completeness not implied |
| Shared CLI and scoped GG Coder integration; beginner delivery | Real CLI subprocess/MCP SDK protocol parity and fixed startup capabilities | Actual host registration prompt dismissed, no settings changed or host smoke test; browser UI/launcher and non-CLI beginner delivery remain later |
| Bounded framework jobs and duplicate-safe recovery | Actual installed framework with owned fixture endpoint/child, fenced claims, ledger, retries, deadline/output/cancellation tests | Live calls absent/denied; request-level paid accounting and permitted sources/prices unverified |
| Restart, migration, backup/export/recovery, retention/correction | Real-file fault tests, new-destination semantic recovery and removal-ledger tests, latest timed exact-table comparison | Authoritative newest ledger is a trusted selection; real-data retention, encryption, expiry and legal erasure deferred |
| Credential containment and narrow tools | Sentinel/fault checks, sanitized worker environment, bounded schemas, no shell/filesystem/admin tools | Not OS sandbox/security certification; no hosted tenancy, so cross-workspace isolation is conditional, not passed |
| Contracts/setup/failure/storage docs, tests and minimal infrastructure | Root setup, canonical reconciliation, fixture report and configured CI; no extra database/workflow platform | No hosted CI/Linux result; redistribution zstd notices unresolved; broader security/accessibility/usability/release gates remain |

## Remaining gates and phase status

The foundation phase remains **in progress**. No live providers, real customer records or completed market-advice product are delivered. Actual GG Coder host smoke testing requires explicit registration permission; the dismissed prompt is not consent. UI/launcher, consented beginner comprehension, independent recommendation-quality review, live price/source authorization, real-data retention/encryption and release/security/legal/accessibility review remain outside this fixture implementation. The zstd wrapper/WASM notice/provenance gate persists before redistribution. Windows results do not establish Linux support. No Roadmap state or host settings were changed by this documentation update.
